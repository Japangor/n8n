---
interface Props {
  code: string;
  language?: string;
}

const { code, language = 'bash' } = Astro.props;
---

<pre class="overflow-x-auto rounded-xl border border-slate-700 bg-slate-900 p-4"><code class={`language-${language}`}>{code}</code></pre>
